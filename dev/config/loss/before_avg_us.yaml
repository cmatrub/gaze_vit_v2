# @package _global_
trainer:
  loss:
    _target_: losses.ce_plus_gaze_reg.CEBeforeAvgUS
    reg_lambda: 1.0

data_pipeline:
  preprocess_gaze:
    _target_: utils.patch_gaze_masks.normalize_gaze_masks
    _partial_: true
    patch_size: ["${patch_size}", "${patch_size}"]

  load_dataset:
    use_gaze: True
